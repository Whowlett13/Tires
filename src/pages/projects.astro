---
import BaseLayout from "../layouts/BaseLayout.astro";
import Link from "../components/ui/link.astro";
import Icon from "../components/ui/Icon.astro";
import Hero from "../components/Hero.astro";
import RepoCard from "../components/RepoCard.astro";









// Local JSON data with images and descriptions
// const localProjects = [
//   {
//   name: "One Track Productions",
//   description:"One Track Productions is a vibrant and dynamic company dedicated to curating unforgettable musical experiences that transcend boundaries and elevate the spirit. Since our inception in 2023, we have been at the forefront of the live music industry, orchestrating events that resonate with music enthusiasts worldwide.",
//   image: "/content/images/OneTrackPro.png",
// },
// ];

// function displayLocalProjects(projects) {
//   return projects.map((project) => {
//     return (
//       <div class="project">
//         <img src={project.image} alt={project.name} />
//         <h3>{project.name}</h3>
//         <p>{project.description}</p>
//       </div>
//     );
//   });
// }
---

<BaseLayout
  title="Projects | Wyatt Howlett"
  description="Learn about Wyatt Howlett's most recent projects"
>
  <div class="stack gap-20">
    <header class="wrapper hero">
      <Hero
        align="center"
        title="My Projects"
        tagline="Showcase of my most recent projects"
      />
      <img
        alt="illustration of a men coding web projects"
        src="/assets/HowltechSolutions.jpg"
      />
    </header>

    <main class="wrapper stack gap-10">
      <div class="repos">
        
          <RepoCard
            
          />
        
       
      </div>
      
      <div class="links">
        <Link
          class="link"
          href="https://github.com/whowlett13"
          style="rounded"
          external
        >
          <Icon icon="code" size="1.33em" /> See All Projects
        </Link>
        <Link class="link" href="/contact" style="roundedInverted">
          <Icon icon="readCV" size="1.33em" /> See Resume
        </Link>
      </div>
    </main>
  </div>
</BaseLayout>

<style lang="scss">
  .hero {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding-inline: 2.5rem;
    gap: 1.5rem;

    img {
      width: 100%;
      max-width: 22rem;
      border-radius: 50%;
    }
  }

  .repos {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  .links {
    display: flex;
    flex-direction: column;
    gap: 1rem;

    .link {
      width: 100%;
      justify-content: center;
    }
  }

  @media (min-width: 50em) {
    .hero {
      flex-direction: row;
      gap: 6rem;
    }

    .repos {
      grid-template-columns: repeat(auto-fit, minmax(20rem, 1fr));
    }

    .links {
      margin-top: 0.5rem;
      flex-direction: row;
      justify-content: center;
      gap: 0.8rem;

      .link {
        width: auto;
      }
    }
  }
</style>
